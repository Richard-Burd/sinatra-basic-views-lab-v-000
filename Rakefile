ENV["SINATRA_ENV"] ||= "development"

require_relative './config/environment'

# Type `rake -T` on your command line to see the available rake tasks.

desc "This will allow you to use Pry which you love soo much!"
task :console do
  Pry.start
end
